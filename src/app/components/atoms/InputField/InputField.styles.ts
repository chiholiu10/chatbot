import styled from "styled-components";

export const StyledLabel = styled.label`
  position: absolute;
  width: 0.063rem; // 1px
  height: 0.063rem; // 1px
  padding: 0rem;
  margin: -0.063rem;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
`;
